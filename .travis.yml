language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dockermeow/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dockermeow/docker-react npm run test -- --coverage
